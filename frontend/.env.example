# Base URL of the backend API, without a trailing slash.
# Leave empty to run in demo mode (simulated detections, schedules kept in the browser).
VITE_API_URL=

# Set to true to force demo mode even when VITE_API_URL is set.
VITE_USE_MOCK=false

# API
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9
pydantic>=2.6
pillow>=10.0
numpy>=1.26

# Models (needed when PIPELINE=real). Install torch for your machine first if it is not already there
# (it is preinstalled on Colab): https://pytorch.org/get-started/locally/
ultralytics>=8.3
transformers>=4.45
